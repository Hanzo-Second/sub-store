# Address exposed on the Docker host. Keep this on loopback when using Nginx.
SUBSTORE_BIND_ADDRESS=127.0.0.1
SUBSTORE_PORT=8080

# Exact externally visible origin. Do not add a trailing slash.
SUBSTORE_BASE_URL=https://sub.example.com
